<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/surface">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="28dp">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Setup Echo"
            android:textSize="28sp"
            android:textColor="@color/accent"
            android:fontFamily="sans-serif-light"
            android:letterSpacing="0.1"
            android:layout_marginBottom="8dp"/>

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Echo needs a few permissions to work properly"
            android:textSize="14sp"
            android:textColor="@color/text_muted"
            android:layout_marginBottom="28dp"/>

        <!-- Permission status -->
        <TextView
            android:id="@+id/tv_permission_status"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Checking permissions..."
            android:textSize="14sp"
            android:textColor="@color/text"
            android:lineSpacingExtra="6dp"
            android:padding="16dp"
            android:background="@drawable/card_bg"
            android:layout_marginBottom="24dp"/>

        <!-- Core permissions -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="REQUIRED"
            android:textSize="11sp"
            android:textColor="@color/text_dim"
            android:letterSpacing="0.2"
            android:layout_marginBottom="10dp"/>

        <Button
            android:id="@+id/btn_grant_permissions"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Grant Microphone, Calls, Contacts"
            android:paddingVertical="14dp"
            android:backgroundTint="@color/accent"
            android:textColor="@color/white"
            android:layout_marginBottom="10dp"
            android:stateListAnimator="@null"/>

        <Button
            android:id="@+id/btn_notification_permission"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Allow Notifications"
            android:paddingVertical="14dp"
            android:backgroundTint="@color/accent_light"
            android:textColor="@color/text"
            android:layout_marginBottom="24dp"
            android:stateListAnimator="@null"/>

        <!-- Optional but important -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="FOR WHATSAPP, SCREENSHOTS AND SETTINGS"
            android:textSize="11sp"
            android:textColor="@color/text_dim"
            android:letterSpacing="0.2"
            android:layout_marginBottom="10dp"/>

        <Button
            android:id="@+id/btn_accessibility"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Enable Accessibility Service"
            android:paddingVertical="14dp"
            android:backgroundTint="@color/accent_light"
            android:textColor="@color/text"
            android:layout_marginBottom="10dp"
            android:stateListAnimator="@null"/>

        <Button
            android:id="@+id/btn_write_settings"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Allow Write Settings (Brightness)"
            android:paddingVertical="14dp"
            android:backgroundTint="@color/accent_light"
            android:textColor="@color/text"
            android:layout_marginBottom="32dp"
            android:stateListAnimator="@null"/>

        <!-- Continue -->
        <Button
            android:id="@+id/btn_continue"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Continue to Echo →"
            android:paddingVertical="16dp"
            android:backgroundTint="@color/accent"
            android:textColor="@color/white"
            android:textSize="15sp"
            android:stateListAnimator="@null"/>

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="You can also grant these later from Settings"
            android:textSize="12sp"
            android:textColor="@color/text_dim"
            android:layout_gravity="center"
            android:layout_marginTop="12dp"/>

    </LinearLayout>

</ScrollView>
